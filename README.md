To run, in menu make sure the typeface is available (for Windows: use Arial, for Linux use "DejaVu Sans"), then make sure the right file is selected in the mcgb - again Arial for Windows and DejaVu Sans for Linux.
If just is installed, then just run works to run the game, otherwise cd into SpaceDefence and "dotnet run"
